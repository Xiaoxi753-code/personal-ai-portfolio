# 国内服务器迁移指南

这份指南用于将当前 Next.js 网站从 Vercel 迁移到阿里云、腾讯云等可运行 Docker 的 Linux 服务器。此次准备只迁移“网站程序”，Supabase 数据库、Storage 和 Auth 仍继续使用原来的云端项目，不会修改或搬运数据库。

## 0. 当前 Ubuntu 服务器基础信息

本项目当前准备使用以下服务器进行部署：Ubuntu 24.04 LTS、2 核 CPU、2 GB 内存、3 Mbps 公网带宽，登录用户名为 `ubuntu`，公网 IP 为 `43.139.174.186`。

本地 Windows PowerShell 连接命令：

```powershell
ssh ubuntu@43.139.174.186
```

登录服务器后，执行以下命令更新系统并安装 Docker Engine、Docker Compose v2 插件和 Git：

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y ca-certificates curl gnupg git
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo \"$VERSION_CODENAME\") stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin git
sudo systemctl enable --now docker
sudo usermod -aG docker "$USER"
```

执行 `newgrp docker` 或退出 SSH 后重新登录，使用户组变更生效。验证命令：

```bash
git --version
docker --version
docker compose version
sudo systemctl is-active docker
```

预期结果分别为 Git 版本号、Docker 版本号、Compose v2 版本号和 `active`。如果当前用户暂时没有 Docker 权限，可使用 `sudo docker ...` 验证安装状态，再重新登录。

## 1. Docker 在这里做什么

Docker 会把 Node.js、项目构建产物和启动方式封装到标准容器中。服务器只要安装兼容的 Docker Engine，即可按照相同的镜像和启动参数运行网站，减少运行环境差异。

项目已经在 `next.config.ts` 中开启 `output: "standalone"`。Next.js 构建时只会收集生产运行真正需要的文件，Docker 最终镜像不必携带完整开发依赖，因此体积更小。

## 2. 上线前需要准备

1. 购买一台 Linux 云服务器。入门阶段可从 2 核 CPU、2 GB 内存开始，再按实际流量调整。
2. 在云厂商安全组中开放 SSH 端口 `22`，以及网站所需的 `80` 和 `443`。不要把数据库端口随意开放到公网。
3. 在服务器安装 Git 和 Docker Engine，并确保执行 `docker version` 能看到客户端与服务端信息。
4. 准备域名时，将域名解析到服务器公网 IP。中国大陆服务器使用域名公开提供服务通常还需要完成 ICP 备案，具体以云厂商和当地规则为准。
5. 确认服务器能访问 Supabase 和所选大模型 API。国内不同网络线路的延迟与可达性可能不同，购买长期套餐前应先测试。

## 3. 把代码放到服务器

通过 SSH 登录服务器后，克隆仓库并进入项目目录：

```bash
git clone <你的 GitHub 仓库地址>
cd personal-ai-portfolio
```

如果服务器无法稳定访问 GitHub，可以改用合规的代码镜像或手动上传源码压缩包，但不要把 `.env.local`、密钥或构建缓存一起公开上传。

## 4. 准备生产环境变量

在服务器项目目录创建仅由管理员读取的 `.env.production`。变量名参考 `.env.local.example`：

```dotenv
AI_API_KEY=请填写真实值
AI_BASE_URL=请填写真实值
AI_MODEL=请填写真实值
NEXT_PUBLIC_SUPABASE_URL=请填写真实值
NEXT_PUBLIC_SUPABASE_ANON_KEY=请填写真实值
SUPABASE_SERVICE_ROLE_KEY=请填写真实值
```

安全要求：

- `.env.production` 不得提交到 Git，也不要放入 Docker 镜像。
- `SUPABASE_SERVICE_ROLE_KEY` 能绕过 RLS，只能作为容器运行时环境变量交给服务端，绝不能添加 `NEXT_PUBLIC_` 前缀。
- 建议执行 `chmod 600 .env.production`，只允许当前服务器管理员读写。
- `NEXT_PUBLIC_` 变量会在浏览器端使用，不应放置任何 Service Role Key 或其他高权限秘密。

## 5. 构建并启动容器

Next.js 会在构建时把 `NEXT_PUBLIC_` 开头的公开配置写入浏览器代码，因此先把这两个公开变量加载到当前终端，再构建镜像：

```bash
export NEXT_PUBLIC_SUPABASE_URL="你的 Supabase URL"
export NEXT_PUBLIC_SUPABASE_ANON_KEY="你的 anon/publishable key"

docker build \
  --build-arg NEXT_PUBLIC_SUPABASE_URL \
  --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY \
  -t personal-ai-portfolio:latest .

docker run -d \
  --name personal-ai-portfolio \
  --restart unless-stopped \
  --env-file .env.production \
  -p 127.0.0.1:3000:3000 \
  personal-ai-portfolio:latest
```

这两个值本来就会发送到浏览器，不属于高权限秘密；`AI_API_KEY` 和 `SUPABASE_SERVICE_ROLE_KEY` 绝不能作为构建参数，它们只通过 `--env-file .env.production` 在容器运行时注入。构建完成后可执行 `unset NEXT_PUBLIC_SUPABASE_URL NEXT_PUBLIC_SUPABASE_ANON_KEY` 清理当前终端变量。

这里把容器端口只绑定到服务器本机 `127.0.0.1:3000`，避免直接把 Next.js 开发端口暴露到公网。检查运行状态：

```bash
docker ps
docker logs --tail 100 personal-ai-portfolio
curl http://127.0.0.1:3000
```

如果页面能返回 HTML，说明容器已经启动。

## 6. 配置域名与 HTTPS

推荐在容器前面放置 Caddy 或 Nginx 作为反向代理：公网访问 `https://你的域名` 时，由它负责 HTTPS 证书和 80/443 端口，再把请求转发到 `http://127.0.0.1:3000`。

不要直接把 3000 端口暴露给所有访客。启用 HTTPS 后，应测试以下链路：

- 首页、恋爱页和留言板可以打开。
- AI 聊天可以流式回答，且未知资料仍触发统一兜底。
- 管理员可以登录、退出并访问受保护路由。
- 留言、知识库、项目和相册的读取及管理操作正常。
- 浏览器地址栏没有证书警告，手机网络也可以访问。

## 7. 后续更新网站

代码更新后，在服务器执行：

```bash
git pull
export NEXT_PUBLIC_SUPABASE_URL="你的 Supabase URL"
export NEXT_PUBLIC_SUPABASE_ANON_KEY="你的 anon/publishable key"
docker build \
  --build-arg NEXT_PUBLIC_SUPABASE_URL \
  --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY \
  -t personal-ai-portfolio:latest .
docker rm -f personal-ai-portfolio
docker run -d \
  --name personal-ai-portfolio \
  --restart unless-stopped \
  --env-file .env.production \
  -p 127.0.0.1:3000:3000 \
  personal-ai-portfolio:latest
```

删除旧容器不会删除 Supabase 中的数据；容器只是网站程序。正式环境更新前仍建议备份 Supabase，并保留上一个可用镜像标签，以便新版本异常时回滚。

## 8. 从 Vercel 迁移时的检查清单

- [ ] 服务器已安装 Docker，磁盘和内存充足。
- [ ] 六个环境变量全部配置，真实密钥未进入 Git 或镜像。
- [ ] 域名解析、备案（如适用）、80/443 安全组和 HTTPS 已完成。
- [ ] Supabase 与大模型 API 从服务器所在地区可以稳定访问。
- [ ] 前台页面、AI、留言板和后台鉴权均通过测试。
- [ ] 日志中没有输出 API Key、Cookie 或 Service Role Key。
- [ ] 已制定镜像回滚、数据库备份和定期更新方案。

> 提醒：Docker 解决的是应用运行环境的一致性，不会自动解决国内网络延迟、域名备案、数据库迁移或服务端限流。这些项目仍需在正式迁移前分别评估。

## 10.2 Docker 网络加速与容器化部署

### 10.2.1 大陆地区拉取 Docker Hub 超时

中国大陆服务器访问 Docker Hub 可能受到跨境链路延迟、丢包、连接重置或出口带宽限制影响，常见错误包括 `i/o timeout`、`context deadline exceeded` 和 `TLS handshake timeout`。该问题应从服务器网络与 Docker Registry 配置排查，而不是修改项目代码。

### 10.2.2 配置镜像加速源

优先使用云厂商控制台为当前账号和地域提供的镜像加速地址。地址、可用范围和认证要求可能变化，不要直接使用来源不明的公共地址：

```bash
sudo mkdir -p /etc/docker
sudo nano /etc/docker/daemon.json
```

写入以下结构，并将示例地址替换为云厂商控制台给出的实际地址：

```json
{
  "registry-mirrors": [
    "https://<云厂商提供的镜像加速地址>"
  ]
}
```

在 `nano` 中按 `Ctrl+O` 保存、按回车确认文件名，再按 `Ctrl+X` 退出。然后执行：

```bash
sudo systemctl daemon-reload
sudo systemctl restart docker
sudo systemctl enable docker
docker info
```

确认输出中包含 `Registry Mirrors`。如果重启失败，执行 `sudo journalctl -u docker --no-pager -n 100` 检查 JSON 语法和地址格式。

### 10.2.3 环境变量文件的安全隔离

`.env.production` 包含 AI API Key、Supabase Service Role Key 等敏感配置。项目使用 `.gitignore` 中的 `.env*` 规则排除这类文件，因此 `git clone` 后不会出现 `.env.production`，这是预期的凭据安全隔离行为。

可以检查忽略规则：

```bash
git check-ignore -v .env.production
```

不要删除忽略规则，也不要把真实密钥写入 README、Dockerfile、Issue 或 Git 提交记录。

进入项目目录，在服务器侧创建文件：

```bash
cd ~/personal-ai-portfolio
nano .env.production
```

按实际服务填写以下变量。示例值不能直接用于生产环境：

```dotenv
AI_API_KEY=请填写真实的模型服务密钥
AI_BASE_URL=https://你的模型服务地址/v1
AI_MODEL=你的模型名称
NEXT_PUBLIC_SUPABASE_URL=https://你的项目编号.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=请填写公开客户端密钥
SUPABASE_SERVICE_ROLE_KEY=请填写服务端高权限密钥
```

按 `Ctrl+O` 保存、回车确认，再按 `Ctrl+X` 退出。限制文件权限：

```bash
chmod 600 .env.production
ls -l .env.production
grep -E '^[A-Z0-9_]+=' .env.production | sed 's/=.*$/=<已配置>/'
```

最后一条只输出变量名和脱敏标记，不会输出完整密钥。

### 10.2.4 构建与启动容器

`NEXT_PUBLIC_` 变量会在 Next.js 构建阶段写入浏览器代码，所以构建时需要提供两个公开的 Supabase 变量；AI Key 和 Service Role Key 不得作为构建参数：

```bash
export NEXT_PUBLIC_SUPABASE_URL="https://你的项目编号.supabase.co"
export NEXT_PUBLIC_SUPABASE_ANON_KEY="你的公开客户端密钥"

docker build \
  --build-arg NEXT_PUBLIC_SUPABASE_URL \
  --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY \
  -t personal-ai-portfolio:latest .

unset NEXT_PUBLIC_SUPABASE_URL NEXT_PUBLIC_SUPABASE_ANON_KEY
```

使用环境变量文件启动容器：

```bash
docker run -d \
  --name personal-ai-portfolio \
  --restart unless-stopped \
  --env-file .env.production \
  -p 3000:3000 \
  personal-ai-portfolio:latest
```

检查运行状态：

```bash
docker ps
docker logs --tail 100 personal-ai-portfolio
curl http://127.0.0.1:3000
```

### 10.2.5 BUG-006：Docker 构建时前端环境变量丢失的排查

如果容器可以启动，但留言板或管理员登录提示“留言服务尚未配置”或“登录服务尚未配置”，并且浏览器端读取到的 `NEXT_PUBLIC_` 变量为空，通常是构建阶段没有注入公开客户端变量。

Next.js 会在构建客户端 JavaScript 时静态替换 `NEXT_PUBLIC_` 变量。Docker 构建时必须先执行 `export`，再通过 `--build-arg` 传给 Dockerfile；容器运行时的 `.env.production` 只对服务端代码有效，不能修改已经生成的浏览器代码。

正确顺序：

```bash
export NEXT_PUBLIC_SUPABASE_URL="https://你的项目编号.supabase.co"
export NEXT_PUBLIC_SUPABASE_ANON_KEY="你的公开客户端密钥"

docker build \
  --build-arg NEXT_PUBLIC_SUPABASE_URL \
  --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY \
  -t personal-ai-portfolio:latest .

docker run -d \
  --name personal-ai-portfolio \
  --restart unless-stopped \
  --env-file .env.production \
  -p 3000:3000 \
  personal-ai-portfolio:latest
```

`AI_API_KEY` 和 `SUPABASE_SERVICE_ROLE_KEY` 仍然只能通过 `--env-file .env.production` 在运行时注入，不能作为 `--build-arg`，也不能写入 Dockerfile、镜像标签或 Git 提交。

### 10.2.6 BUG-007：修改 `.env.production` 后 `docker restart` 不生效

`docker run --env-file .env.production` 只在创建容器实例时读取环境变量。之后修改宿主机文件，再执行 `docker restart`，Docker 只会重启原容器，不会重新读取该文件。

修改服务端环境变量后，必须停止并删除旧容器，再使用更新后的环境变量文件执行完整的 `docker run`。如果修改的是 `NEXT_PUBLIC_` 变量，还必须先重新构建镜像。

```bash
docker stop personal-ai-portfolio
docker rm personal-ai-portfolio
docker run -d --name personal-ai-portfolio --restart unless-stopped --env-file .env.production -p 3000:3000 personal-ai-portfolio:latest
```

不要在日志或 `docker inspect` 输出中公开完整密钥。

### 10.2.7 端口映射与云平台安全组

`-p 3000:3000` 将服务器主机 TCP 3000 映射到容器 TCP 3000。Docker 负责端口映射，云平台安全组负责是否允许外部访问，两者必须同时满足：

1. 容器处于运行状态并监听容器内 3000 端口。
2. Docker 已建立主机 3000 到容器 3000 的映射。
3. 安全组入方向规则允许 TCP 3000，来源范围按实际需要限制。
4. Ubuntu 主机防火墙（如果启用 UFW）允许 TCP 3000。

确认映射：

```bash
docker port personal-ai-portfolio
sudo ss -lntp | grep ':3000'
```

正式公开网站时，建议 Caddy 或 Nginx 监听 80/443 并反向代理到 `127.0.0.1:3000`，启动参数改为：

```bash
-p 127.0.0.1:3000:3000
```

此时安全组只需开放 SSH、HTTP 和 HTTPS 所需端口，不应额外开放 3000、数据库端口或 Docker 管理端口。
